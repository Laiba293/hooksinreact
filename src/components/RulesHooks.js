//Always write it inside the component or function. 
//Component name must be PascalCase(first letter should be upparcase)
//we can directly import or we can directly write it using React.hookname
//don't call hooks inside loop , condition , or nested function.
// import React, { useState } from 'react'

// const RulesHooks = () => {
//     const[myName,setMyName] = useState('laiba');
//   return (
//     <div>
//       <h1>{myName}</h1>
//     </div>
//   )
// }

// export default RulesHooks;
